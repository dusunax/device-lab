# device-lab

작은 기기와 디스플레이 기능을 검증하는 저장소입니다.

## 범위

- e-paper 기기
- AMOLED 기기
- 기기별 펌웨어 실험
- 하드웨어 설정과 문제 해결 문서

## 기기 목록

| 기기 유형  | 별칭    |  보드 / 모델                         | 시작 월    | 현재 단계       |
| ------- | ------- | ------------------------------- | ------- | ----------- |
| e-Paper | Snow | Waveshare ESP32-S3-ePaper-1.54G | 2026-09 | 상태 카드 기준 구현 |

## 저장소 구조

```text
e-paper/
  ESP32S3/
    snow-status-card/

vendor/
  waveshare_epaper_1in54g/

docs/
  e-paper/
  shared/
```

## 구조 원칙

- 외부 폴더와 PR 제목은 기능 영역 기준으로 이름을 붙입니다.
- 기기 별칭은 코드 내부, 설정 문서, 체크리스트에서 식별용으로 사용합니다.
- e-paper 관련 실행 코드는 `e-paper/` 아래에 둡니다.
- 외부 기반 드라이버 코드는 `vendor/` 아래에 둡니다.
- 문서는 모두 `docs/` 아래에서 관리합니다.
- e-paper 관련 문서는 `docs/e-paper/`에 둡니다.
- 여러 기기에 공통으로 쓰는 문서는 `docs/shared/`에 둡니다.
- 빈 폴더 유지만을 위한 `.gitkeep`은 두지 않습니다.
- 폴더 깊이와 폴더 개수는 실제 필요가 생길 때만 늘립니다.

## 보안 원칙

Wi-Fi 자격정보, 토큰, 로컬 IP 주소, MAC 주소 등 환경 식별 정보는 커밋하지 않습니다.

Git에는 `secrets.example.h`만 포함하고, 실제 로컬 설정인 `secrets.h`는 추적하지 않습니다.
