#pragma once

const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// Used only for weather lookup (Open-Meteo). Seoul by default.
const float WEATHER_LATITUDE = 37.5665f;
const float WEATHER_LONGITUDE = 126.9780f;
